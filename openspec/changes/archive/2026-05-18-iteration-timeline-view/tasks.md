## 1. 数据层改造

- [x] 1.1 修改 `timelineStart` 计算逻辑：从 Planning 的 `createdAt` 取值，而非任务参与者的最早时间
- [x] 1.2 修改 `timelineEnd` 计算逻辑：从 Planning 的 `deadline` 取值，无 deadline 时 fallback 到 `createdAt + 14天`
- [x] 1.3 新增 `daySlots` 计算属性：将时间范围转换为每天 4 个格子的数组结构

## 2. 网格布局

- [x] 2.1 重构模板结构：左侧任务信息列 + 右侧 CSS Grid 时间网格
- [x] 2.2 实现网格表头：每天 4 列，列头显示日期（如"5月18日"）
- [x] 2.3 实现网格竖线：每天 4 格之间的分隔线，日期之间的加粗分隔线

## 3. 任务行与横条渲染

- [x] 3.1 实现任务行渲染：每行显示任务标题、阶段标签、负责人
- [x] 3.2 实现参与者横条定位：根据 startTime/endTime 计算横条在网格中的起止列位置
- [x] 3.3 实现多参与者横条垂直堆叠：同一任务行内多个参与者横条按行堆叠
- [x] 3.4 横条按角色着色：复用现有 roleColors 映射

## 4. 筛选功能

- [x] 4.1 添加任务阶段筛选器（复用 TASK_STAGES 常量）
- [x] 4.2 添加优先级筛选器
- [x] 4.3 将筛选逻辑接入 filteredTasks 计算属性

## 5. 样式与交互

- [x] 5.1 实现左侧任务信息列 sticky 定位（横向滚动时固定）
- [x] 5.2 实现网格表头 sticky 定位（纵向滚动时固定）
- [x] 5.3 横条 hover 效果：显示参与者角色和姓名的 tooltip
- [x] 5.4 空状态处理：无任务时显示提示信息

## 6. 边界情况处理

- [x] 6.1 处理迭代无 deadline 的情况：使用默认 14 天范围
- [x] 6.2 处理任务无参与者时间数据的情况：行内不显示横条
- [x] 6.3 处理时间跨度较大时的最小列宽保证
